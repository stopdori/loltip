항상 한국어로 답변해줘

## 매치업 파일 추가/수정 후 워크플로우

`app/data/matchups/**/*.ts` (매치업 데이터 파일)을 새로 추가하거나 내용을 수정한 뒤에는
**반드시 `npm run gen:matchup-json`을 실행**해야 한다.

- `app/data/matchups/_index.ts`의 `getMatchupSummary`는 개별 매치업 파일을 직접 import하지 않고,
  `app/data/matchups/_compiled.json`(전체 매치업 데이터를 병합한 파일)을 `fs.readFileSync` + `JSON.parse`로 읽어서 조회한다.
- `_compiled.json`은 `scripts/gen-matchup-json.ts`가 `app/data/matchups` 하위 `.ts` 파일들을 스캔해서 생성하는
  **자동 생성 파일**이며, 소스 `.ts` 파일을 고쳐도 `_compiled.json`을 재생성하지 않으면 실제 서비스(런타임)에는 반영되지 않는다.
- 이 구조는 매치업 라우트(`/matchup/[pair]`, `/champ/[id]`)의 빌드 시 파일 트레이스가 14,000개+ 파일로 폭증해서
  Vercel 빌드가 OOM으로 실패하는 문제를 해결하기 위해 도입됨. `getMatchupSummary`를 파일별 동적 import 방식으로
  되돌리지 말 것.

**절차**
1. `app/data/matchups/<champA>/<champA>_<champB>.ts` 파일 추가/수정
2. `npm run gen:matchup-json` 실행 → `app/data/matchups/_compiled.json` 갱신 확인
   (또는 `npm run watch:matchup`을 미리 켜두면 `.ts` 파일 저장 시 자동으로 재생성됨)
3. `_compiled.json`도 함께 커밋 (자동 생성 파일이지만 git에 커밋되는 대상임 — gitignore 대상 아님)

## 로컬 개발 환경 주의사항

이 프로젝트는 로컬에서 `npm run dev`(개발 서버)와 `npm run watch:matchup`(매치업 파일 자동 감시,
저장 시 `_compiled.json` 자동 재생성)이 **항상 켜져 있는 상태로 작업하는 경우가 많다.**

- 클로드 코드가 로컬에서 `npm run build`, `npm run dev`, `next start` 등을 **직접 실행하기 전에는**,
  먼저 bash로 포트 3000이 이미 사용 중인지 확인할 것(예: `netstat -ano | grep 3000` 또는 유사 명령).
- 이미 실행 중인 서버가 있다면 직접 새 서버를 띄우지 말고 다음 중 하나로 진행할 것:
  a) 격리된 워크트리(`git worktree`)에서 빌드/테스트를 진행한다.
  b) 다른 포트를 사용한다 (`next dev -p 3080` 등).
  c) 사용자에게 "로컬 서버가 켜져 있는 것 같은데, 확인 목적으로 이러이러한 작업을 진행해도 되는지" 먼저 물어본다.
- `.next` 디렉토리의 락 파일(`.next/dev/lock`) 문제로 이전에 충돌이 발생한 적이 있다.
  기존 dev 서버를 임의로 종료(kill)하지 말 것 — 사용자가 직접 켜둔 세션일 수 있다. 종료가 꼭 필요하면
  먼저 사용자에게 확인받을 것.

## 매치업 파일 부분 커밋 워크플로우

일부 매치업 파일만 완성되어 커밋 대상이고, 다른 매치업 파일들은 아직
작업 중(WIP)이라 이번 커밋에 포함하면 안 되는 상황에서 반드시 아래 순서를 따를 것:

1. git status로 현재 수정된 파일 전체를 확인하고, 그중 이번에
   커밋할 파일과 커밋하지 않을(WIP) 파일을 구분한다.
2. 커밋하지 않을 WIP 파일들만 골라서 `git stash push -- <파일 경로들>`로
   임시 대피시킨다. (커밋 대상 파일은 stash에 포함하지 않음)
3. `npm run gen:matchup-json`을 실행해 _compiled.json을 재생성한다.
   이 시점엔 디스크에 커밋 대상 파일들만 남아있어야 하므로,
   _compiled.json에는 커밋 대상 내용만 반영된다.
4. 커밋 대상 .ts 파일들과 _compiled.json을 함께
   git add → git commit → git push 한다.
5. 커밋 직후 반드시 `git stash pop`으로 2번에서 대피시킨 WIP 파일들을 복원한다.
6. `git stash list`를 실행해 남아있는 stash가 없는지 최종 확인한다.
   비어있어야 이 워크플로우가 정상 종료된 것이다.
   만약 stash가 남아있다면 즉시 사용자에게 알리고 pop을 시도한다.

이 워크플로우는 반드시 5번(stash pop)과 6번(최종 확인)까지 완료해야
작업이 끝난 것으로 간주한다. 5번을 생략하면 WIP 파일의 수정사항이
유실된 것처럼 보이는 문제가 생기므로 절대 건너뛰지 않는다.

## 스타일/레이아웃 테스트는 dev-preview 페이지를 사용할 것

스타일/레이아웃 테스트가 필요할 때는 `/ko/dev-preview` 페이지(`app/[locale]/dev-preview/page.tsx`)를
사용할 것 - 이 페이지는 매치업 시스템(CHAMPIONS, sitemap 등)과 완전히 분리된 독립 페이지라
실제 데이터 손상 위험이 없음. 실제 매치업 데이터 파일에 테스트 문장을 임시로 넣었다 빼는
방식은 다른 작업 내용을 실수로 손상시킬 위험이 있으므로 사용하지 말 것.

## 검증/확인 관련 규칙

코드 수정 작업 완료 후, 명시적으로 요청받지 않은 이상 브라우저 접속(claude-in-chrome),
스크린샷 캡처, 시각적 확인 등의 검증 작업을 하지 말 것. 사용자가 직접 눈으로 확인하는 걸
선호하며, 불필요한 검증은 토큰 낭비로 간주한다.

- 허용되는 것: `tsc --noEmit`, lint 등 코드 레벨의 정적 검증은 계속 수행해도 됨
  (이건 검증이 아니라 코드 정합성 확인 단계로 간주).
- 금지되는 것: 프롬프트에 "확인해줘", "검증해줘" 같은 명시적 요청이 없으면,
  브라우저를 열어 렌더링 결과를 스크린샷으로 확인하거나 DOM을 조회하는 작업은 하지 말 것.

## 작업 후 자가 검증 규칙 (예외 없는 기본 동작)

데이터/설정 파일(tags.ts, tagCategories.ts, statIcons.ts, tagTone.ts
등)에서 값을 추가/삭제/변경하는 작업을 마치면, "지시한 대로 됐다"고
보고하기 전에 반드시 실제 값을 다시 조회해서 확인한다.

- 삭제를 지시받았으면: 그 항목이 undefined/존재하지 않음을 직접
  조회해서 확인한다 (보고만 하고 확인을 생략하지 않는다).
- 추가/변경을 지시받았으면: 그 항목의 실제 값을 다시 읽어서 지시받은
  값과 정확히 일치하는지 비교한다 (문자열이면 완전 일치, 객체면
  필드 단위로).
- 두 항목이 "동일해야 한다"고 지시받았으면: 각각의 값을 조회해서
  실제로 값을 비교하는 코드(===, 배열이면 깊은 비교)를 실행해
  결과를 보고한다 — 눈대중이나 추측으로 "같을 것"이라고 보고하지
  않는다.
- 타입 변경이 있었던 작업이면 npx tsc --noEmit도 함께 실행한다.

이 검증은 사용자가 매번 명시적으로 요청하지 않아도 항상 수행하고,
검증 결과(비교 항목과 실제 값)를 보고에 포함한다. 검증에서 불일치가
발견되면 "완료했다"고 보고하지 않고 즉시 원인을 밝히고 수정한다.

## 검증 방법 선택 원칙

자가 검증을 할 때, 무조건 스크래치 스크립트(임시 .ts 파일 작성 →
ts-node로 실행 → 삭제)부터 쓰지 않는다. 확인하려는 게 다음에 해당하면
더 가벼운 방법(grep, Read, 간단한 bash 명령)으로 먼저 시도한다:

- 어떤 항목이 파일에 존재하는지/삭제됐는지 확인 → grep으로 충분
- 두 값(문자열, 짧은 표현)이 동일한지 비교 → Read로 두 값을 나란히
  확인하거나 grep으로 충분
- 특정 키가 배열/객체에 포함되어 있는지 확인 → grep으로 충분

아래처럼 실제 런타임 평가나 타입 레벨 확인이 필요한 경우에만
스크래치 스크립트(또는 tsc --noEmit)를 쓴다:
- 여러 배열의 합집합/차집합처럼 grep만으로 계산하기 어려운 로직 검증
- 타입이 실제로 올바르게 추론되는지 확인
- 함수의 실행 결과(렌더링 시뮬레이션 등)를 재현해야 하는 경우

즉 "검증이 필요하다"가 곧바로 "스크립트를 짜야 한다"는 뜻은 아니다 —
매번 이 확인에 맞는 가장 가벼운 도구가 뭔지 먼저 판단한다.

## tagCategories.ts에 태그를 새로 추가할 때의 배치 규칙

TAG_CATEGORIES 또는 GIMMICK_CATEGORIES의 어떤 카테고리에 새 태그를
추가해야 할 때, 아무 생각 없이 최상위 keys 배열 끝에만 덧붙이고
끝내지 않는다. 반드시 아래 순서로 진행한다:

1. 그 카테고리의 기존 subGroups들(title과 그 안의 keys)을 훑어서,
   새 태그와 의미가 가장 비슷한(같은 성격의 태그들이 이미 모여있는)
   subGroup을 찾는다.
2. 그 subGroup의 keys 배열에 새 태그를 추가한다. 마땅히 맞는
   subGroup이 없으면 사용자에게 물어보거나, 가장 근접한 곳에 넣고
   왜 그렇게 판단했는지 보고한다 — 임의로 새 subGroup을 만들지
   않는다.
3. 같은 작업 안에서 반드시 그 카테고리의 최상위 keys 배열에도 동일한
   태그를 추가한다. subGroups에만 추가하고 최상위 keys를 빠뜨리는
   실수, 또는 그 반대(최상위 keys에만 추가하고 subGroups를 빠뜨리는
   실수) 둘 다 금지한다.
4. 작업 후 그 카테고리의 "subGroups 키 합집합 === 최상위 keys"
   정합성을 확인한다(가벼운 방법으로 — grep이나 직접 비교).

이 규칙은 새 태그 추가뿐 아니라, 기존 태그를 한 subGroup에서 다른
subGroup으로 옮기는 작업에도 동일하게 적용된다 — 이동 후에도 최상위
keys 자체는 변하지 않아야 하며(같은 카테고리 안에서의 이동이라면),
다른 카테고리로 이동하는 경우엔 원래 카테고리의 keys에서 빼고 새
카테고리의 keys에 넣는 것도 같은 동작으로 처리한다.

## 로그 저장 방침

기본적으로 로그 파일을 자동 저장하지 않는다. 사용자가 특정 결과를
파일로 저장해달라고 명시적으로 요청한 경우에만
logs/YYYY-MM-DD_HHmm_<작업명>.txt에 저장한다 (logs/는 .gitignore
등록됨).

## 답변 분량 표기 규칙

채팅에 어떤 답변이든 작성하면, 그 답변 맨 끝에 실제로 작성한 답변
본문의 총 글자 수(공백 포함, 한글 기준)를 아래 형식으로 반드시
덧붙인다:

[답변 길이: 약 XXX자]

정확한 카운트 도구가 없다면 대략적인 추정치도 괜찮다 — 목적은
사용자가 복붙한 내용이 이 숫자와 크게 다르면(예: 절반 이하) 중간에
텍스트가 누락됐다는 걸 바로 알아챌 수 있게 하는 것이다.

## 챔피언 skillTooltip 작성 규칙

- skillTooltip(P/Q/W/E/R 아이콘 호버 툴팁)을 채울 때, DDragon 자동 fetch
  결과를 초기값으로 삼되, 그 안에 "0"이나 명백히 이상한 값(위치 매핑
  오류로 의심되는 값)이 있으면 절대 그대로 두지 말고 위키
  (wiki.leagueoflegends.com — Riot 공식 후원 위키, 검색 스니펫 여러 개를
  조합하지 말고 해당 챔피언 페이지를 직접 fetch해서 하나의 최신 소스로
  확인할 것)로 정확한 수치를 찾아서 채운다.
- 검색 스니펫은 서로 다른 캐시 시점이 섞여 날짜가 다른 정보가 충돌할 수
  있으니, 반드시 해당 챔피언의 공식 위키 페이지(wiki.leagueoflegends.com/
  en-us/[챔피언명])를 fetch해서 패치 히스토리 섹션 최상단(가장 최근 패치)
  까지 확인하고, 그 페이지 하나에 나온 능력치 설명과 수치를 최종 값으로
  채택한다.
- DDragon의 vars/effectBurn이 완전히 비어있거나(예: 뽀삐 W 사례)
  위치 매핑이 명백히 꼬여있는 스킬은, placeholderOverrides로 숫자만
  땜질하지 않는다 — skillTooltip에 문장 전체를 위키 기준으로 새로 써서
  하드코딩하는 것이 기본 방침이다.
- skillTooltip 문장 안에서 ultCooldown처럼 챔피언 파일의 다른 필드와
  겹치는 숫자는 직접 타이핑하지 않고 `{{ultCooldown}}` 같은
  플레이스홀더로 참조한다(`SkillTagsPanel.tsx`의
  `resolveSkillTooltipPlaceholders`가 렌더링 시점에 실제 값으로 치환).
  같은 숫자가 여러 필드에 중복 타이핑되면 한쪽만 패치 반영하고 다른
  쪽을 깜빡할 위험이 생기므로, 항상 숫자의 "원본 필드"가 어디인지
  먼저 정하고 나머지는 그 필드를 참조하게 만든다.
- skillTooltip을 새로 작성/재작성할 때는 해당 챔피언 파일에 이미 적혀
  있는 기존 데이터(skills/vision/gimmick/notes/skillTooltip 등)를
  참고하지 않는다. 기존 내용이 맞든 틀리든 곁눈질하지 말고, 오직
  DDragon + 위키만을 근거로 처음부터 새로 조사해서 작성한다(기존 값에
  기대어 "이미 이렇게 써있으니 맞겠지"로 재사용하는 것 금지).
- skillTooltip 작업 중 "KO를 나중에 더 다듬을 예정"이라고 미리 밝힌
  경우, 그 시점에는 EN을 함께 작성하지 않는다(빈 값으로 두거나 기존
  값 유지). KO가 확정된 뒤 "KO 수정 마무리됐다"는 별도 지시가 오면
  그때 최종 KO 기준으로 EN을 동기화해서 채운다 — 지금 단계에서 EN을
  써봤자 KO가 또 바뀌면서 이중작업이 되는 걸 방지하기 위함. 단, 이
  예외는 사용자가 이렇게 미리 밝힌 경우에만 적용되고, 별다른 언급이
  없으면 기존 방침대로 ko/en을 함께 작성한다.
- 문장을 다 쓴 뒤에는 DDragon/위키에서 가져온 수치·개념 중 아직
  `[[TAG]]`로 치환하지 않고 그냥 텍스트로 남아있는 게 있는지 한 번 더
  훑어서 재점검한다(피해량 종류, 스킬 형태, 상태이상, 판정 방식, 스탯
  비례 등 tags.ts/tags_gimmick.ts에 이미 있는 개념을 빠뜨리는 실수가
  반복된 적 있음).

## 챔피언 개별 파일(app/data/champs/*.ts) 구조 기준 — poppy.ts

`app/data/champs/poppy.ts`를 챔피언 파일 구조의 레퍼런스로 삼는다.
새 챔피언 파일을 만들거나 기존 파일을 리팩터링할 일이 생기면, 아래
patterns을 poppy.ts와 동일하게 맞춘다:

- `skillTooltip`(P/Q/W/E/R 호버 툴팁 하드코딩 문장, 위 "챔피언
  skillTooltip 작성 규칙" 참고)을 채워서 DDragon 실시간 fetch 대신
  우선 쓰이게 한다.
- 문장 안에 `[[TAG]]` 토큰을 적극적으로 넣어서 TokenText로 렌더링되게
  한다(숫자·개념 명사를 태그로 치환 + 괄호로 부가정보 표기 + 문장이
  조밀해지는 지점엔 `\n`으로 가독성 확보).
- 궁극기 쿨타임처럼 다른 구조화 필드와 겹치는 숫자는 위 규칙대로
  `{{필드명}}` 플레이스홀더로 참조한다.
- `placeholderOverrides`는 예외적인 경우에만 쓰고 기본은 `skillTooltip`
  하드코딩.

**진행 상태**: 아직 poppy.ts 외 다른 챔피언 파일들은 이 구조로
마이그레이션되지 않았다(전부 옛 방식 그대로). 한번에 전체를 바꾸는
작업은 하지 않는다 — 각 챔피언 파일을 어차피 손댈 일이 생길 때마다
이 구조에 맞춰 점진적으로 정렬해나간다.
